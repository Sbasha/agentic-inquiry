# Explanation

*Understanding-oriented* user docs per [Diátaxis](https://diataxis.fr/)
— why a design works the way it does, what concepts mean, how systems
fit together.

This directory is **empty pending the first migration.** Today, the
explanation-shaped content is mixed across
[`../../architecture/`](../../architecture/) (internal-facing structural
explanations) and [`../../design/`](../../design/) (normative specs that
include "why" prose).

**First-fill target.** Lift the conceptual portions of
[`../../architecture/search.md`](../../architecture/search.md) (hybrid
ranking rationale, IDF-weighted boosting motivation) into
`explanation/hybrid-search.md` — the reference-shaped pieces stay where
they are.

See [`../../CONVENTIONS.md § 5c`](../../CONVENTIONS.md#5c-docsguides--for-users).

> **When you make the first move into this directory**, rewrite this
> file: drop the "empty pending the first migration" framing, replace
> the first-fill target with a real index of what lives here, and add
> the new doc to the README's documentation table.
