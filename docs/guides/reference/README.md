# Reference

*Information-oriented* user docs per [Diátaxis](https://diataxis.fr/) —
authoritative, dry, complete descriptions of interfaces, config,
commands.

This directory is **empty pending the first migration.** Today, the
reference content lives under
[`../../api-reference/`](../../api-reference/) (programmatic Python
API) and [`../../design/`](../../design/) (normative specs: filter AST,
query semantics, result contract, schema reference).

**First-fill target.** Move [`../../api-reference/api.md`](../../api-reference/api.md)
to `reference/api.md` the next time it is substantively updated, and
add a `reference/cli.md` for the `ai` CLI (currently undocumented
outside `agentic_inquiry/cli/`).

See [`../../CONVENTIONS.md § 5c`](../../CONVENTIONS.md#5c-docsguides--for-users).

> **When you make the first move into this directory**, rewrite this
> file: drop the "empty pending the first migration" framing, replace
> the first-fill target with a real index of what lives here, and add
> the new doc to the README's documentation table.
