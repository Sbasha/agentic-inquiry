{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SQLite Backend Configuration",
    "description": "Configuration schema for SQLite storage backend",
    "type": "object",
    "properties": {
        "type": {
            "const": "sqlite",
            "description": "Backend type identifier"
        },
        "database_path": {
            "type": "string",
            "description": "Path to SQLite database file",
            "minLength": 1
        },
        "table_prefix": {
            "type": "string",
            "description": "Prefix for table names (useful for multi-tenant deployments)",
            "pattern": "^[a-zA-Z0-9_]*$"
        }
    },
    "required": ["type", "database_path"],
    "additionalProperties": false
}
