{
  "name": "agv",
  "version": "1.5.0",
  "description": "Agent-Vault - Semantic Search, Onboarding, and Codebase Intelligence",
  "mcpServers": {
    "agv-daemon": {
      "command": "python3",
      "args": ["-m", "extensions.gemini.agv.servers.daemon", "--workspace", "${workspacePath}"],
      "cwd": "${extensionPath}"
    }
  }
}
