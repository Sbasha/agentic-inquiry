description = "List all agv commands and their descriptions"
prompt = """
List all agv commands and their descriptions

Usage: /agv:help [command]

# agv:help

Quick reference for all Agent-Vault commands.

## All Commands

### Getting Started
| Command | Description |
|---------|-------------|
| `agv:onboard` | Intelligent onboarding - parallel index, explore, validate |
| `agv:setup` | Configure storage backend (local/gcp/alloydb) |
| `agv:status` | Show current project, index, and mode status |
| `agv:env` | Manage isolated environments |

### Search & Discovery
| Command | Description |
|---------|-------------|
| `agv:index` | Index codebase for semantic search |
| `agv:search` | Semantic search across code and docs |
| `agv:search similar <entity>` | Find code similar to an entity |
| `agv:entity` | Understand a code entity |
| `agv:entity deps <name>` | Show entity dependencies |
| `agv:entity refs <name>` | Show entity references |

### Analysis
| Command | Description |
|---------|-------------|
| `agv:patterns` | Discover architectural patterns |
| `agv:lineage` | Trace data flow |
| `agv:lineage gaps` | Find entities without full traceability |
| `agv:impact` | Analyze change blast radius |
| `agv:services` | Map service architecture |
| `agv:validate` | Verify index accuracy |

### Knowledge
| Command | Description |
|---------|-------------|
| `agv:memory save` | Save a project insight |
| `agv:memory recall` | Search saved memories |
| `agv:memory list` | List all memories |

> **Dev tools** (testing, code review, server management) are in the `agv-dev` plugin.

## Quick Start

```bash
agv:setup local myproject   # 1a. Configure (local)
agv:setup alloydb myproject # 1b. Configure (AlloyDB - server-side embeddings)
agv:index .                 # 2. Index
agv:search "auth flow"      # 3. Search
```

## Get Help on Specific Command

```bash
agv:help search    # Details about agv:search
agv:help entity    # Details about agv:entity
```

## Output Formats

Most commands support `--json` for machine-readable output:
```bash
agv:search "query" --json
agv:entity MyClass --json
```

## Dynamic Discovery

Commands are discovered dynamically from the plugin cache. List all available:
```python
from agent_vault.cli.discover import discover_agv_commands
for cmd in discover_agv_commands():
    print(f"{cmd.full_name}: {cmd.description}")
```

## More Info

- `agv:status` - Check current state
- README at `extensions/claude/README.md`
- Full docs at `AGENTS.md`

User Arguments: {{args}}

Based on the documentation above and the provided arguments, execute the appropriate `agv` CLI command(s) using the available tools.
"""
