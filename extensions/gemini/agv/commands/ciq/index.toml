description = "Index a codebase for semantic search. Use when setting up agv for a project, updating the index after changes, or checking index status."
prompt = """
Index a codebase for semantic search. Use when setting up agv for a project, updating the index after changes, or checking index status.

Usage: /agv:index [path] [--project-id name] [status]

# /agv:index

Index code and documentation for semantic search.

## Quick Start

```bash
/agv:index .                    # Index current directory
/agv:index src/                 # Index specific path
/agv:index status               # Show index statistics
```

## Options

| Option | Description |
|--------|-------------|
| `path` | Directory to index (default: `.`) |
| `--project-id` | Project identifier for isolation |
| `status` | Show current index stats |

## Examples

```bash
# /agv:index
/agv:index . --project-id myproject

# /agv:index
/agv:index src/ --project-id myproject

# /agv:index
/agv:index status --project-id myproject
```

## CLI Equivalent

```bash
uv run --env-file .env agv index . --project-id myproject
uv run --env-file .env agv index status --project-id myproject
```

## What Gets Indexed

- **Code**: Functions, classes, methods with relationships
- **Docs**: Markdown, text files, docstrings
- **Structure**: Import graphs, call hierarchies

## Index Statistics

The `status` subcommand shows:
- Total entities indexed
- Breakdown by type (functions, classes, etc.)
- Last index timestamp
- Storage size

User Arguments: {{args}}

Based on the documentation above and the provided arguments, execute the appropriate `agv` CLI command(s) using the available tools.
"""
