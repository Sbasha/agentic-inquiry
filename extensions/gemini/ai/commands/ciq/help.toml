description = "List all ai commands and their descriptions"
prompt = """
List all ai commands and their descriptions

Usage: /ai:help [command]

# ai:help

Quick reference for all Agentic Inquiry commands.

## All Commands

### Getting Started
| Command | Description |
|---------|-------------|
| `ai:onboard` | Intelligent onboarding - parallel index, explore, validate |
| `ai:setup` | Configure local storage (LanceDB) |
| `ai:status` | Show current project, index, and mode status |
| `ai:env` | Manage isolated environments |

### Search & Discovery
| Command | Description |
|---------|-------------|
| `ai:index` | Index codebase for semantic search |
| `ai:search` | Semantic search across code and docs |
| `ai:search similar <entity>` | Find code similar to an entity |
| `ai:entity` | Understand a code entity |
| `ai:entity deps <name>` | Show entity dependencies |
| `ai:entity refs <name>` | Show entity references |

### Analysis
| Command | Description |
|---------|-------------|
| `ai:patterns` | Discover architectural patterns |
| `ai:lineage` | Trace data flow |
| `ai:lineage gaps` | Find entities without full traceability |
| `ai:impact` | Analyze change blast radius |
| `ai:services` | Map service architecture |
| `ai:validate` | Verify index accuracy |

### Knowledge
| Command | Description |
|---------|-------------|
| `ai:memory save` | Save a project insight |
| `ai:memory recall` | Search saved memories |
| `ai:memory list` | List all memories |

> **Dev tools** (testing, code review, server management) are in the `ai-dev` plugin.

## Quick Start

```bash
ai:setup local myproject   # 1. Configure (local LanceDB)
ai:index .                 # 2. Index
ai:search "auth flow"      # 3. Search
```

## Get Help on Specific Command

```bash
ai:help search    # Details about ai:search
ai:help entity    # Details about ai:entity
```

## Output Formats

Most commands support `--json` for machine-readable output:
```bash
ai:search "query" --json
ai:entity MyClass --json
```

## Dynamic Discovery

Commands are discovered dynamically from the plugin cache. List all available:
```python
from agentic_inquiry.cli.discover import discover_agv_commands
for cmd in discover_agv_commands():
    print(f"{cmd.full_name}: {cmd.description}")
```

## More Info

- `ai:status` - Check current state
- README at `extensions/claude/README.md`
- Full docs at `AGENTS.md`

User Arguments: {{args}}

Based on the documentation above and the provided arguments, execute the appropriate `ai` CLI command(s) using the available tools.
"""
