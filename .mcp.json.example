{
  "mcpServers": {
    "ai-sse": {
      "url": "http://localhost:8765/sse"
    },
    "ai-test": {
      "command": "uv",
      "args": [
        "run",
        "ai",
        "--config", "config/test-lancedb.yaml",
        "--project-id", "test",
        "--transport", "stdio"
      ]
    }
  }
}
