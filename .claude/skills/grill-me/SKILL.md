---
name: grill-me
disable-model-invocation: true
description: "Interview the user one decision at a time when explicitly asked to stress-test a plan or idea. Optionally ground the interview in the project's existing glossary and decision records. Do not enact the plan, author a delivery contract or publish tickets as a side effect."
metadata:
  source: Inspired by https://github.com/mattpocock/skills
---

# Grill me

When the user wants the interview grounded in existing terminology or durable decisions, read [documentation-grounded interviewing](references/grill-with-docs/reference.md). Use the project's own glossary and decision-record locations; the included formats are optional examples. Preserve existing write authorization and return proposed edits when it does not cover those sources. Ordinary interviews need not create documents.

Use `frame-intent` to author an intent, `de-risk-intent` to test an assumption, `decompose-intent` to shape child delivery contracts, and `to-tickets` to project an already approved plan into tracker-sized work.


Interview me relentlessly about every aspect of this until we reach a shared understanding. Walk down each branch of the decision tree, resolving dependencies between decisions one-by-one. For each question, provide your recommended answer.

If a _fact_ can be found by exploring the environment (filesystem, tools, etc.), look it up rather than asking me. The _decisions_, though, are mine - put each one to me and wait for my answer.

Use the multiple choice question answer tool for questions with a discrete set of options.

Ask the questions one at a time, waiting for feedback on each question before continuing. Asking multiple questions at once is bewildering.

Do not act on it until I confirm we have reached a shared understanding.
