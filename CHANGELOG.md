# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Local-only codebase intelligence runtime: LanceDB and SQLite storage,
  local embeddings, hybrid search, code and document indexing, memory,
  the `ai` command, the stdio MCP server and the Claude Code and Gemini
  extensions.
- `docs/storage-backends.md`: the storage provider contract and what an
  external database provider for governed projects must add.

## [0.1.0] - 2026-09-12

- Public preview of the earlier `ai` library (tag `v0.1.0`).
